import { z } from "zod";
import { buildJsonSchemas } from "fastify-zod";

// export const { schemas: voucherSchemas, $ref } = buildJsonSchemas({
//   createVoucherSchema,
//   updateVoucherSchema,
// });
